# Skeleton machine for Solo on top of imx6qsabresd until board-specific DTS/U-Boot are ported.
require conf/machine/imx6qsabresd.conf

MACHINEOVERRIDES =. "solo-imx6:"

# TODO: replace with Solo-specific DTB once ported.
KERNEL_DEVICETREE:imx6qsabresd = "imx6q-sabresd.dtb"

# Keep classic image names until Solo recipes are migrated.
IMAGE_FSTYPES += "tar.gz"
